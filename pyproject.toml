[project]
name = "cineo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "django>=5.2.9",
    "python-dotenv>=1.2.1",
    "ruff>=0.15.1",
]
