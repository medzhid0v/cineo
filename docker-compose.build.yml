## Сборка образа локально вместо тянущегося из GHCR (для разработки/проверки).
## Использование:
##   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/medzhid0v/cineo:local

  celery:
    image: ghcr.io/medzhid0v/cineo:local
